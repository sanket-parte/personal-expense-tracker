/**
 * Services barrel export
 *
 * API services, storage abstractions, and external integrations.
 * As the app grows, add modules here:
 *   - api.ts        → HTTP client setup (axios/fetch wrapper)
 *   - storage.ts    → AsyncStorage / MMKV wrappers
 *   - analytics.ts  → Analytics service
 */
